package com.fbdblog.dao;

import com.fbdblog.dao.hibernate.BasePersistentClass;
import com.fbdblog.dao.hibernate.RegerEntity;
import com.fbdblog.dao.hibernate.HibernateUtil;
import com.fbdblog.util.GeneralException;
import com.fbdblog.dao.hibernate.AuthControlled;
import org.apache.log4j.Logger;

/**
 * Blogger generated by hbm2java
 */

public class Questionconfig extends BasePersistentClass implements java.io.Serializable, RegerEntity, AuthControlled {


    // Fields
     private int questionconfigid;
     private int questionid;
     private String name;
     private String value;


    //Validator
    public void validateRegerEntity() throws GeneralException {

    }

    //Loader
    public void load(){

    }

    public static Questionconfig get(int id) {
        Logger logger = Logger.getLogger("com.fbdblog.dao.Questionconfig");
        try {
            logger.debug("Questionconfig.get(" + id + ") called.");
            Questionconfig obj = (Questionconfig) HibernateUtil.getSession().get(Questionconfig.class, id);
            if (obj == null) {
                logger.debug("Questionconfig.get(" + id + ") returning new instance because hibernate returned null.");
                return new Questionconfig();
            }
            return obj;
        } catch (Exception ex) {
            logger.error("com.fbdblog.dao.Questionconfig", ex);
            return new Questionconfig();
        }
    }

    // Constructors

    /** default constructor */
    public Questionconfig() {
    }


    public boolean canRead(User user){
        Question question = Question.get(questionid);
        return question.canRead(user);
    }

    public boolean canEdit(User user){
        return canRead(user);
    }



    // Property accessors

    public int getQuestionconfigid() {
        return questionconfigid;
    }

    public void setQuestionconfigid(int questionconfigid) {
        this.questionconfigid = questionconfigid;
    }

    public int getQuestionid() {
        return questionid;
    }

    public void setQuestionid(int questionid) {
        this.questionid = questionid;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getValue() {
        return value;
    }

    public void setValue(String value) {
        this.value = value;
    }


}
